# Elections Service MVP - Environment Configuration Template
# Copy this file to .env and fill in actual values
# DO NOT commit .env to git (contains secrets)

# Server Configuration
PORT=8081
NODE_ENV=development

# Database Configuration (Cloud SQL PostgreSQL 15)
# Local development: Use Cloud SQL Proxy
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5433
DATABASE_NAME=postgres
DATABASE_USER=postgres
DATABASE_PASSWORD=<get-from-secret-manager>

# Production: Use Unix socket
# DATABASE_HOST=/cloudsql/ekklesia-prod-10-2025:europe-west2:ekklesia-db
# DATABASE_PORT=5432

# Database Connection Pool (optimized for 300 votes/sec spike)
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=5
DATABASE_POOL_IDLE_TIMEOUT=30000
DATABASE_POOL_CONNECTION_TIMEOUT=2000

# S2S Authentication (shared with Events service)
S2S_API_KEY=<generate-random-secret>

# CORS Configuration
CORS_ORIGINS=https://ekklesia-prod-10-2025.web.app,https://ekklesia-prod-10-2025.firebaseapp.com,http://localhost:3000

# Logging
LOG_LEVEL=info

# Election Configuration (MVP: hardcoded)
ELECTION_TITLE=Prófunarkosning 2025
ELECTION_QUESTION=Do you support this proposal?

# To get database password from Secret Manager:
# gcloud secrets versions access latest --secret=postgres-password --project=ekklesia-prod-10-2025

# To generate S2S API key:
# openssl rand -hex 32
