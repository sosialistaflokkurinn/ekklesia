# Events service — local development
# Copy to .env: cp .env.local.example .env
# Requires: docker compose up -d (PostgreSQL on port 5433)

DATABASE_HOST=127.0.0.1
DATABASE_PORT=5433
DATABASE_NAME=postgres
DATABASE_USER=socialism
DATABASE_PASSWORD=localdev

FIREBASE_PROJECT_ID=ekklesia-prod-10-2025

S2S_API_KEY=local-dev-key
ELECTIONS_SERVICE_URL=http://localhost:8082

ADMIN_RESET_ENABLED=true
ALLOWED_RESET_UIDS=local-dev-uid

NODE_ENV=development
PORT=8080
CORS_ORIGINS=http://localhost:5000
STORAGE_BUCKET=ekklesia-prod-10-2025.firebasestorage.app
