# Python dependencies for ZITADEL authentication
# Add these to your pyproject.toml or requirements.txt

# OIDC and JWT handling
python-jose[cryptography]==3.3.0
requests==2.31.0
cryptography==41.0.7

# For Morepath integration
morepath==0.19

# Session handling (if not already included)
beaker==1.12.1  # Or your preferred session library

# Environment variable handling
python-dotenv==1.0.0

# Optional: For better security
argon2-cffi==23.1.0  # For password hashing if needed
pyotp==2.9.0  # For 2FA if needed