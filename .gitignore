# This is your personal .gitignore file for local-only rules.
# It is NOT committed to Git.
# Project-wide rules are in .gitignore.example.

# --- PERSONAL & LOCAL RULES ---

# IDE / Tooling specific folders
/.claude/
/.gemini/
/.playwright-mcp/

# Sensitive temporary documentation (contains PII/kennitalur)
*KENNITALA*.md
*DUPLICATE_SSN*.md
*DJANGO_KENNITALA*.md
*kennitala*.md
*pii_redaction*.md

# Local-only scripts
validate_documentation_map.py

# Local Claude settings
.claude/settings.local.json

# Example SQL files (database reset - local testing only)
reset-election.example.sql
*-reset*.sql
*-example.sql

# Local data files
data/Stadfangaskra.csv