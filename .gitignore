# =============================================================================
# Ekklesia .gitignore
# =============================================================================
# STRATEGY: "Track All, Push Selectively" with credential exception
#
# - Generated/build files: gitignored (not tracked)
# - Credential files (.env): gitignored (NEVER tracked - security)
# - PII/sensitive docs: tracked locally, blocked from push by pre-push hook
#
# See .git-local-only for patterns blocked from push.
# =============================================================================

# === Generated / Build Artifacts ===
node_modules/
dist/
build/
.next/
out/

# === Python Generated ===
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# === Virtual Environments ===
venv/
env/
.venv/
.virtualenv/

# === IDE / Editor ===
# Note: .vscode/ is TRACKED (shared workspace settings)
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# === OS Specific ===
.DS_Store
Thumbs.db

# === Firebase / GCP ===
.firebase/
cloud-sql-proxy*

# === Logs (runtime logs, not audit logs) ===
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Temporary Build Files ===
*.backup
*.bak
*.old

# === Generated Metadata ===
.eslintcache

# === Environment Files with Secrets ===
# NEVER track .env files - they contain database passwords and API keys
**/.env
.env
.env.local
.env.development
.env.production
.env.staging
# ALLOW templates (no secrets):
!**/.env.example
!**/.env.yaml

# === Playwright MCP ===
.playwright-mcp/

# === Symlinks (created by setup scripts) ===
services/svc-members/public
services/members/public

# === Admin Portal Build Output ===
public/admin/

# =============================================================================
# IMPORTANT:
# - Credentials (.env files): gitignored above - NEVER tracked
# - PII documents: tracked locally but blocked from push (see .git-local-only)
# =============================================================================
