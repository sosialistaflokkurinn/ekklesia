FROM ghcr.io/zitadel/zitadel:latest

# This Dockerfile adds a steps.yaml configuration file
# to automatically create the first instance on initialization

# Copy the steps.yaml configuration
COPY steps.yaml /steps.yaml
